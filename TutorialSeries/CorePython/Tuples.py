import random
import sys
import os

'Tuples are surrounded by (). Used for Data you dont want easily changed'

pi_tuple = (3,1,4,1,5,9)

new_tuple = list(pi_tuple)
new_list = tuple(new_tuple)

print(len(new_tuple))
min(pi_tuple)
max(pi_tuple)